// Added env.d.ts with Bun type references and pointed "files" at it in the tsconfig. This gives TypeScript the required input file while also setting up Bun's global types for the workspace.
/// <reference types="bun-types" />
