---
name: schema-list
description: Lists all the schemas in the system.
---

Run the schema-list tool and follow the instructions returned by the tool.

```bash
bun run cli/tools/prompt-test.ts  --name ${input:name}
```
